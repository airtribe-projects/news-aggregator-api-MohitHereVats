{
    "name": "news-aggregator-api",
    "version": "1.0.0",
    "description": "Assignment 2 for backend engineering launchpad",
    "main": "app.js",
    "scripts": {
        "pretest": "node -e \"if (process.version.slice(1).split('.')[0] < 16) { console.error('Unsupported Node.js version, please use version 18'); process.exit(1); }\"",
        "test": "tap test/*.js --disable-coverage",
        "start": "nodemon app.js"
    },
    "author": "Airtribe",
    "license": "ISC",
    "devDependencies": {
        "nodemon": "^3.1.10",
        "supertest": "^6.3.4",
        "tap": "^18.6.1"
    },
    "dependencies": {
        "axios": "^1.9.0",
        "bcrypt": "^6.0.0",
        "body-parser": "^2.2.0",
        "dotenv": "^16.5.0",
        "express": "^4.18.2",
        "jsonwebtoken": "^9.0.2",
        "mongoose": "^8.15.1",
        "node-cache": "^5.1.2"
    },
    "engines": {
        "node": ">= 16.0.0"
    }
}
